<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fiftyeightmorris.nailbiter">

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.fiftyeightmorris.nailbiter.AlertActivity"
            android:allowEmbedded="true"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault.Light" />

        <receiver
            android:name="com.fiftyeightmorris.nailbiter.AlertNotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.fiftyeightmorris.nailbiter.SHOW_NOTIFICATION" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.fiftyeightmorris.nailbiter.PositionMonitorService"
            android:enabled="true" />

    </application>

</manifest>
